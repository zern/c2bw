# 智能圖像預處理工具 (v3.7) 用戶操作使用說明

---

## 一、軟體簡介與免安裝特性

**智能圖像預處理工具（c2bw）** 是一款專為古籍善本、掃描書籍、學術文獻及檔案數位化設計的批次預處理與重構工具。

- **雙版本按需選用（單檔案綠色免安裝）**：無需安裝 Python 或額外環境，按兩下即用：
  - **預設版（推薦 Windows 10 / 11 64 位元）**：`智能圖像預處理工具 v3.7.exe`，基於現代化 WebView2 / EdgeChromium 渲染引擎，UI 流暢細膩；
  - **Windows 7 相容版（適用於 Windows 7 / 8 / 8.1 及老舊系統）**：`智能圖像預處理工具 v3.7_Win7.exe`，內建老舊系統執行庫與 MSHTML / IE11 適配核心，支援環境受損時無感自動降級至 Tkinter 原生桌面介面，兼具極致穩定性；
- **全格式相容**：原生支援 JPG、JPEG、PNG、TIF、TIFF、BMP、JP2（JPEG 2000）等圖像格式，以及掃描圖片封裝型的 PDF 文件；
- **純本機離線安全**：執行過程中不產生任何外部網路請求，自動向作業系統核心申請臨時隨機閒置連接埠，支援同時多開執行，無連接埠衝突；
- **高畫質與極致壓縮**：採用工業級 1 位元 CCITT Group 4 演算法二值化與底層 PDF 資料流直通封裝，百頁黑白 PDF 僅數百 KB，字跡銳利且白底黑字絕不反相；
- **多語言自動適配**：原生支援簡體中文、繁體中文、日本語、English。啟動時自動識別系統語言，未符合時預設使用簡體中文；亦可隨時在右上角手動切換並自動記憶；任務執行記錄檔及輸出報告（`task_report.txt`）同步依所選語言生成。

---

## 二、快速上手：兩種工作模式

開啟程式後，介面頂部可直接切換兩種工作模式：

```
[ 從圖片目錄開始處理 ]    [ 從PDF文件開始處理 ]
```

### 模式 1：從圖片目錄開始處理（推薦常規掃描圖批次處理）

適用於處理已匯出的成批掃描圖片。

#### 操作步驟：
1. **指定輸入目錄**：
   - 點擊 **【瀏覽】** 選擇圖片資料夾；或者**直接從 Windows 檔案總管將資料夾拖入程式介面**。
   - *(可選)* 勾選 **“處理子資料夾內的檔案”**：如包含多級子目錄，程式會自動遞迴處理並完整保留原有目錄層級結構。
2. **指定輸出目錄**：
   - 預設會自動在輸入目錄下建立 `output` 資料夾；亦可點擊【瀏覽】或直接拖入自訂資料夾。
3. **設定處理參數**（詳見後文“參數設定詳解”）：
   - 根據需要開啟或關閉 **【色彩處理】**（黑白二值化 / 檔案大小優化：原大圖片、精簡尺寸適合手機、自訂參數）；
   - 根據需要開啟或關閉 **【分頁處理】**（雙頁智慧拆分 / 自動跳過封面）。
4. **選擇是否合成 PDF**：
   - 在底部執行設定中勾選 **“完成後合併輸出為 PDF”**；
   - 若勾選，預設在生成 PDF 後自動清理中間處理圖片；如需同時保留處理後的圖片檔案，可勾選 **“合成PDF後保留處理後的圖片”**。
5. **開始處理**：
   - 點擊右下角 **【開始處理】**，進度條即時顯示當前處理進度與檔案名稱；
   - 任務完成後會自動彈出處理統計明細，並在輸出目錄儲存 `task_report.txt`。

---

### 模式 2：從 PDF 文件開始處理（掃描版 PDF 重構與提取）

專門用於直接對圖片型 PDF（如掃描古籍、舊版期刊、插圖漫畫等）進行分頁提取與批次重構。

> **注意**：本模式僅針對由圖片生成的 PDF（掃描件）；非掃描的向量純文字排版類 PDF 請勿使用本模式。

#### 常用場景與操作：

- **場景 A：僅從 PDF 無損提取全部原始圖片（提取神器）**
  1. 拖入或選擇 PDF 檔案；
  2. 保持【色彩處理】與【分頁處理】處於**關閉**狀態；
  3. 勾選 **“不轉換為PDF (僅保留處理後的圖片檔案)”**；
  4. 點擊 **【開始處理】**：軟體將全自動提取 PDF 內部的所有原始圖片到同名資料夾，不損失任何畫質。

- **場景 B：掃描 PDF 裁切並二值化為高畫質極小體積新 PDF（文件重構）**
  1. 拖入或選擇 PDF 檔案；
  2. 開啟【分頁處理】（將跨頁拆為單頁）及【色彩處理】（轉為清爽黑白二值圖）；
  3. 保持“不轉換為PDF”為**未勾選**狀態；
  4. 點擊 **【開始處理】**：程式流水線式提取圖片、拆頁、二值化，並重新合併為一份標準化單頁檢視的新 PDF，處理完畢後自動清理臨時解壓縮的圖片。

- **場景 C：掃描 PDF 拆頁處理，但僅需輸出圖片資料夾**
  1. 拖入或選擇 PDF 檔案；
  2. 開啟【分頁處理】或【色彩處理】；
  3. 勾選 **“不轉換為PDF (僅保留處理後的圖片檔案)”**；
  4. 點擊 **【開始處理】**：輸出目錄將儲存處理後的全部圖片，不封裝為 PDF。

---

## 三、核心功能與參數詳解

### 1. 色彩處理與檔案大小優化

| 選項 | 適用場景與效果 |
| :--- | :--- |
| **關閉色彩處理**<br>（檔案大小優化） | **原大圖片（無優化）**：保留原始檔案格式與圖像尺寸，未裁切圖片完整保留原檔；<br>**精簡尺寸（適合手機）**：在裁切前使用最優演算法（Lanczos）將圖片縮小至 2160px 寬（寬度 ≤2160px 不放大，保持原比例），輸出為品質 75 的漸進式（Progressive）JPEG，兼顧行動裝置閱讀清晰度與極致檔案體積；<br>**自訂參數**：使用者可自選圖片縮放比例（80%、60%、50%、40%、30%、20%）及自訂漸進式 JPEG 品質數值（預設 80）。 |
| **開啟色彩處理**<br>（黑白二值化） | **OTSU 自適應**（推薦）：由演算法自動分析頁面光影與墨跡深度，自適應計算最佳分界閾值；<br>**自訂閾值**（0%~100%）：根據掃描件濃淡手動微調。<br>*說明：二值化後統一輸出為標準 1 位元 Group 4 TIFF，文字銳利平滑，徹底消除紙張泛黃底灰。* |

### 2. 分頁處理（雙頁智慧裁切）

針對書籍跨頁掃描（一張圖片包含左、右兩頁）設計，配有即時動態線框預覽：

- **排除單頁比例（寬高比）**（預設 `1.2`）：
  - 判定公式：圖片寬度 ÷ 圖片高度。
  - 當長寬比小於此閾值時，程式自動識別該圖片為封面、封底或單頁插圖，**自動跳過裁切**，原樣保留；
  - 大於等於此閾值時，自動執行雙頁拆分。
- **分割比例（P%）**（預設 `50%`）：
  - 設定為 **50%** 時：從中線平均等分，左、右各占原圖 50% 寬度；
  - 設定為 **大於 50%**（如 `52%`）：左、右各取原圖 52% 寬度，中間自動產生 **4% 的中縫重疊保護區**，可有效避免書籍裝訂線處的文字被切除；
  - 設定為 **小於 50%**（如 `48%`）：中間產生裁切留白縫隙。
- **閱讀順序**：
  - **從右到左**：適合古籍善本、港台直排書籍，右半頁標為第一頁（`_A`），左半頁標為第二頁（`_B`）；
  - **從左到右**：適合現代橫排版書籍，左半頁為 `_A`，右半頁為 `_B`。

### 3. 執行設定

- **最大線程數**：預設依電腦實體核心數配置，可自由設定（1~64）。多線程並行處理極大加快批次高解析度圖的處理效率；
- **暫停與取消**：
  - 處理過程中可隨時點擊 **【暫停】**，再次點擊可無縫繼續；
  - 點擊 **【取消任務】** 會安全終止後續排隊，保護當前已寫入磁碟檔案的完整性。

---

## 四、實用技巧與注意事項

1. **直接拖曳支援**：
   - 可以在任何時候將**圖片資料夾**或 **.pdf 檔案**直接拖入軟體視窗，無需手動逐層點選目錄。
2. **輸出目錄保護**：
   - 為避免原圖被覆蓋，輸出目錄**不能**與輸入目錄相同，也不能是輸入目錄的上層目錄。
3. **生成的 PDF 特性說明**：
   - 若開啟了二值化並輸出 PDF，程式使用底層標準 CCITT Group 4 傳真流封裝，不僅體積超小，且內建閱讀器黑白底色校準，在 Adobe Acrobat、Edge、Chrome、SumatraPDF 等主流閱讀器中均顯示為標準白底黑字；
   - 輸出 PDF 預設內建了“單頁適合視窗”（SinglePage / Fit）檢視指令，開啟即為最佳閱讀比例。
4. **任務記錄檔備查**：
   - 每次任務結束，輸出目錄下都會自動生成一份 `task_report.txt`，清楚記錄處理開始時間、所用參數、排除單頁數、裁切雙頁數及最終生成總頁數，便於建檔校對。
5. **版本選擇與系統相容性**：
   - **Windows 11 / Windows 10 使用者**：請優先選用 `智能圖像預處理工具 v3.7.exe`（預設版），享受流暢的現代化互動；
   - **Windows 7 / 8 / 8.1 使用者**：請選用 `智能圖像預處理工具 v3.7_Win7.exe`（Win7 相容版），自帶環境修補程式與容災降級機制。
6. **精簡尺寸（適合手機）與裁切連動說明**：
   - 當同時勾選【精簡尺寸（適合手機）】與【雙頁裁切】時，程式會在裁切前使用高品質 Lanczos 重採樣演算法將跨頁大圖等比壓縮為 2160px 寬（小於 2160px 寬度的圖片保持不變），隨後精確執行中線拆分與中縫重疊保護。拆分後的單頁自動保持等比縮放且輸出為 Progressive JPEG，兼顧手機高解析度螢幕閱讀體驗與極小檔案體積。
