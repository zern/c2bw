# 智能图像预处理工具 (v3.5) 用户操作使用说明

---

## 一、软件简介与免安装特性

**智能图像预处理工具（c2bw）** 是一款专为古籍善本、扫描书籍、学术文献及档案数字化设计的批量预处理与重构工具。

- **单文件绿色免安装**：无需安装 Python 或任何第三方环境，双击直接运行；
- **全格式兼容**：原生支持 JPG、JPEG、PNG、TIF、TIFF、BMP、JP2（JPEG 2000）等图像格式，以及扫描图片打包型的 PDF 文档；
- **纯本地离线安全**：运行过程中不产生任何外部网络请求，自动向操作系统内核申请临时随机空闲端口，支持同时多开运行，无端口冲突；
- **高画质与极致压缩**：采用工业级 1 位 CCITT Group 4 算法二值化与底层 PDF 数据流直通封装，百页黑白 PDF 仅数百 KB，字迹锐利且白底黑字绝不反相；
- **多语言自动适配**：原生支持简体中文、繁體中文、日本語、English。启动时自动识别系统语言，未匹配时默认使用简体中文；亦可随时在右上角手动切换并自动记忆；任务运行日志及输出报告（`task_report.txt`）同步按所选语言生成。

---

## 二、快速上手：两种工作模式

打开程序后，界面顶部可直接切换两种工作模式：

```
[ 从图片目录开始处理 ]    [ 从PDF文件开始处理 ]
```

### 模式 1：从图片目录开始处理（推荐常规扫描图批量处理）

适用于处理已导出的成批扫描图片。

#### 操作步骤：
1. **指定输入目录**：
   - 点击 **【浏览】** 选择图片文件夹；或者**直接从 Windows 资源管理器将文件夹拖入程序界面**。
   - *(可选)* 勾选 **“处理子文件夹内的文件”**：如包含多级子目录，程序会自动递归处理并完整保留原有目录层级结构。
2. **指定输出目录**：
   - 默认会自动在输入目录下创建 `output` 文件夹；亦可点击【浏览】或直接拖入自定义文件夹。
3. **配置处理参数**（详见后文“参数设置详解”）：
   - 根据需要开启或关闭 **【色彩处理】**（黑白二值化 / 保持原格式 / 转为 JPG）；
   - 根据需要开启或关闭 **【分页处理】**（双页智能拆分 / 自动跳过封面）。
4. **选择是否合成 PDF**：
   - 在底部执行设置中勾选 **“完成后合并输出为 PDF”**；
   - 若勾选，默认在生成 PDF 后自动清理中间处理图片；如需同时保留处理后的图片文件，可勾选 **“合成PDF后保留处理后的图片”**。
5. **开始处理**：
   - 点击右下角 **【开始处理】**，进度条实时显示当前处理进度与文件名；
   - 任务完成后会自动弹出处理统计明细，并在输出目录保存 `task_report.txt`。

---

### 模式 2：从 PDF 文件开始处理（扫描版 PDF 重构与提取）

专门用于直接对图片型 PDF（如扫描古籍、旧版期刊、插图漫画等）进行分页提取与批量重构。

> **注意**：本模式仅针对由图片生成的 PDF（扫描件）；非扫描的矢量纯文字排版类 PDF 请勿使用本模式。

#### 常用场景与操作：

- **场景 A：仅从 PDF 无损提取全部原始图片（提取神器）**
  1. 拖入或选择 PDF 文件；
  2. 保持【色彩处理】与【分页处理】处于**关闭**状态；
  3. 勾选 **“不转换为PDF (仅保留处理后的图片文件)”**；
  4. 点击 **【开始处理】**：软件将全自动提取 PDF 内部的所有原始图片到同名文件夹，不损失任何画质。

- **场景 B：扫描 PDF 裁切并二值化为高清极小体积新 PDF（文档重构）**
  1. 拖入或选择 PDF 文件；
  2. 开启【分页处理】（将跨页拆为单页）及【色彩处理】（转为清爽黑白二值图）；
  3. 保持“不转换为PDF”为**未勾选**状态；
  4. 点击 **【开始处理】**：程序流水线式提取图片、拆页、二值化，并重新合并为一份标准化单页视图的新 PDF，处理完毕后自动清理临时解压的图片。

- **场景 C：扫描 PDF 拆页处理，但仅需输出图片文件夹**
  1. 拖入或选择 PDF 文件；
  2. 开启【分页处理】或【色彩处理】；
  3. 勾选 **“不转换为PDF (仅保留处理后的图片文件)”**；
  4. 点击 **【开始处理】**：输出目录将保存处理后的全部图片，不打包为 PDF。

---

## 三、核心功能与参数详解

### 1. 色彩处理

| 选项 | 适用场景与效果 |
| :--- | :--- |
| **关闭色彩处理** | **保持原格式与品质**：保留原始文件格式（JPG/PNG/JP2/TIFF 等）及编码参数，不重压缩；<br>**转为 JPG（质量 80）**：统一转换为通用 JPG 格式，体积适中，适合网络分发。 |
| **开启色彩处理**<br>（黑白二值化） | **OTSU 自适应**（推荐）：由算法自动分析页面光影与墨迹深度，自适应计算最佳分界阈值；<br>**自定义阈值**（0%~100%）：根据扫描件浓淡手动微调。<br>*说明：二值化后统一输出为标准 1 位 Group 4 TIFF，文字锐利平滑，彻底消除纸张发黄底灰。* |

### 2. 分页处理（双页智能裁切）

针对书籍跨页扫描（一张图片包含左、右两页）设计，配有实时动态线框预览：

- **排除单页比例（宽高比）**（默认 `1.2`）：
  - 判定公式：图片宽度 ÷ 图片高度。
  - 当长宽比小于此阈值时，程序自动识别该图片为封面、封底或单页插图，**自动跳过裁切**，原样保留；
  - 大于等于此阈值时，自动执行双页拆分。
- **分割比例（P%）**（默认 `50%`）：
  - 设定为 **50%** 时：从中线平均等分，左、右各占原图 50% 宽度；
  - 设定为 **大于 50%**（如 `52%`）：左、右各取原图 52% 宽度，中间自动产生 **4% 的中缝重叠保护区**，可有效避免书籍装订线处的文字被切除；
  - 设定为 **小于 50%**（如 `48%`）：中间产生裁切留白缝隙。
- **阅读顺序**：
  - **从右到左**：适合古籍善本、港台竖排书籍，右半页标为第一页（`_A`），左半页标为第二页（`_B`）；
  - **从左到右**：适合现代横排版书籍，左半页为 `_A`，右半页为 `_B`。

### 3. 执行设置

- **最大线程数**：默认按电脑物理核心数配置，可自由设定（1~64）。多线程并发处理极大加快批量高分辨率图的处理效率；
- **暂停与取消**：
  - 处理过程中可随时点击 **【暂停】**，再次点击可无缝继续；
  - 点击 **【取消任务】** 会安全终止后续排队，保护当前已写入磁盘文件的完整性。

---

## 四、实用技巧与注意事项

1. **直接拖拽支持**：
   - 可以在任何时候将**图片文件夹**或 **.pdf 文件**直接拖入软件窗口，无需手动层层点击选择目录。
2. **输出目录保护**：
   - 为避免原图被覆盖，输出目录**不能**与输入目录相同，也不能是输入目录的上级目录。
3. **生成的 PDF 特性说明**：
   - 若开启了二值化并输出 PDF，程序使用底层标准 CCITT Group 4 传真流封装，不仅体积超小，且内置阅读器黑白底色校准，在 Adobe Acrobat、Edge、Chrome、SumatraPDF 等主流阅读器中均显示为标准白底黑字；
   - 输出 PDF 默认内置了“单页适合窗口”（SinglePage / Fit）视图指令，打开即为最佳阅读比例。
4. **任务日志备查**：
   - 每次任务结束，输出目录下都会自动生成一份 `task_report.txt`，清楚记录处理开始时间、所用参数、排除单页数、裁切双页数及最终生成总页数，便于建档校对。
