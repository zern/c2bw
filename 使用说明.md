# 智能图像预处理工具 v3.0 操作说明

## 一、启动程序

双击 `c2bw_win7.exe` 启动程序。程序支持 Windows 7 SP1 及以上系统。

## 二、选择目录

1. 在“输入目录”中选择需要处理的图片文件夹。
2. 在“输出目录”中选择结果保存位置；留空时自动使用输入目录同级的 `output` 文件夹。
3. 如需处理下级文件夹，勾选“处理子文件夹内的文件”。输出时会保留目录结构。

支持的图片格式包括 JPG、JPEG、PNG、TIF、TIFF、BMP 和 JP2。

## 三、设置处理方式

### 色彩处理

- 开启开关：执行黑白二值化，输出适合文字扫描的黑白 TIFF。
- 关闭开关：仅执行裁切时，可选择保持原始格式与品质，或转换为 JPG 质量 80。

### 分页处理

- 开启开关：按设置的比例裁切双页图片。
- 关闭开关：每张图片直接作为一页输出。
- “从右到左 / 从左到右”用于设置页面阅读顺序。

### 执行设置

- “最大线程数”：控制同时处理的图片数量，通常建议设置为 4～8。
- “完成后合并输出为 PDF”：勾选后自动生成 PDF；不勾选时，图片处理完成后会询问是否生成 PDF。

## 四、开始处理

点击“开始处理”后，进度条会显示图片处理进度。

- “暂停”：暂停后续任务，可点击“继续”恢复。
- “取消任务”：停止处理并删除本次输出目录。
- PDF 阶段会重新从 0% 开始显示“正在打包 PDF”进度。

任务结束后，程序会显示结果摘要，并可自动打开输出目录。

## 五、PDF 输出规则

- 未处理子文件夹：生成与输入文件夹同名的 PDF。
- 处理子文件夹：每个子文件夹单独生成一个 PDF，PDF 放在对应子目录的同级目录。
- PDF 默认以单页布局打开，并自动适合页面显示。
- PDF 应用程序名固定为 `SHUGE.ORG`。

## 六、版本更新提示

程序启动时会从服务器读取最新版本信息。发现新版本时，右上角会出现铃铛图标；鼠标移到图标上可查看更新内容，并点击“下载最新版”。

网络不可用时，程序不会弹出更新错误提示。

## 七、注意事项

- 输出目录应为空目录，避免覆盖已有文件。
- 输出目录不能位于输入目录内部，也不能与输入目录相同。
- 处理大量图片时，请确保磁盘有足够空间。
- Windows 7 用户建议安装 SP1、Internet Explorer 11 和必要的系统运行库。
